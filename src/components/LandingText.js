import React from "react";

const LandingText = () => {
  return (
    <div className="landing-text-container">
      <h2>Search for a</h2>
      <img src={require("../img/pokemon-logo.svg")} alt="pokemonlogo"></img>
    </div>
  );
};

export default LandingText;
